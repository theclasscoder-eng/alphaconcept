# Build resources

Place packaging resources here:

- `icon.ico` — Windows application icon (256×256 recommended). If absent,
  electron-builder falls back to the default Electron icon.

This directory is referenced by `electron-builder.yml` (`buildResources: assets`).
